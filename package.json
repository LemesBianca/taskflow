{
  "name": "taskflow",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- -p 3000\"",
    "build": "npm run build --prefix backend ; npm run build --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend -- -p 3000"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
